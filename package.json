{
  "name": "build-plugin",
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "check-sync": "node scripts/check-sync.js",
    "test": "node --test scripts/transformers/*.test.js"
  }
}
